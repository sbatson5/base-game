import failureDialogue from 'base-game/utils/failure-dialogue';
import { module, test } from 'qunit';

module('Unit | Utility | failure dialogue');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = failureDialogue();
  assert.ok(result);
});
